# КБП В (КБПВ-18) — Додавання документу в Посібник

## Overview

Додати повноцінний документ "Курс бойової підготовки вертольотів" (КБП В, КБПВ-18) в систему Посібника (Guide) для перегляду пілотами.

**Мета:** Пілоти вертольотів мають мати доступ до повного тексту КБПВ-18 через веб-інтерфейс Посібника з можливістю перегляду всіх розділів, таблиць та додатків.

**Поточний стан:**
- ✅ Документ існує в `guide_documents` (id=6)
- ✅ 33 розділи в `guide_sections` (4 accordion + 29 content)
- ✅ 292 вправи вже в таблиці `exercises`
- ✅ 48 записів періодів перерв в `break_periods_lp`
- ✅ 83 PNG зображення таблиць в `web/public/images/tables/`
- 📁 Оригінали: `docs/КБП-В-2018/` (файли .doc, .xls)

## Technical Context

### Джерельні файли
```
docs/КБП-В-2018/
├── Зміст КБП.doc                    # Зміст документа
├── Зворот.doc                       # Зворотній білет
├── 1,2 ПРОГРАМА.doc                 # Програми 1, 2
├── 3, 4 ПРОГРАМА.doc                # Програми 3, 4
├── 4-вода, 5 ПРОГРАМА.doc           # Програма 4-вода, 5
├── 5 ПРОГРАМА Борттехніки.doc       # Програма 5 (борттехніки)
├── ПРОГРАМА 6 з ОНВ.doc             # Програма 6 (ОНВ)
├── 1, 2 ЗАДАЧА.doc                  # Завдання 1, 2 (228 сторінок!)
└── ДОДАТКИ/
    ├── Додаток 1.xls                # Таблиці вправ (Excel)
    ├── Додаток 1 продовження.xls    # Морська авіація
    ├── Додаток 2, 3.doc             # Додатки 2, 3
    ├── Додаток 4/
    │   ├── Додаток 4.doc            # Додаток 4 (53 сторінки)
    │   └── таблиці 5, 9, 13.doc     # Окремі таблиці
    ├── Додаток 5.doc
    └── Додаток6.doc
```

### База даних
- **document_id = 6** в `guide_documents`
- Таблиця `guide_sections` — порожня для цього документу
- Формат контенту: Markdown

### Інструкція
Дотримуватись `GUIDE_INSTRUCTION.md`:
- ✅ 100% ідентичність оригіналу
- ❌ НЕ скорочувати контент
- ❌ НЕ використовувати base64 для зображень
- ✅ Таблиці → PNG файли в `/images/tables/`

## Stories

### Story 1: Екстракція тексту з .doc файлів
**Опис:** Конвертувати всі .doc файли в текстовий формат для подальшої обробки.

**Завдання:**
- [x] Встановити інструмент для читання .doc (наприклад, Python `python-docx` або `antiword`)
- [x] Створити скрипт `scripts/extract_kbpv_text.py`
- [x] Екстрагувати текст з кожного .doc файлу в `docs/КБП-В-2018/`
- [x] Зберегти як `docs/КБПВ-18_text.txt` (єдиний файл з маркерами розділів)

**Done:** Файл `docs/КБПВ-18_text.txt` містить повний текст документу з розміткою розділів.

---

### Story 2: Аналіз структури документу
**Опис:** Визначити повну ієрархію розділів для внесення в `guide_sections`.

**Завдання:**
- [x] Проаналізувати `docs/КБПВ-18_text.txt`
- [x] Визначити структуру:
  - Загальні положення (якщо є)
  - Програми 1-6
  - Завдання (ЗАДАЧА 1, 2)
  - Додатки 1-6
- [x] Скласти список всіх розділів з типом (accordion/content)
- [x] Порахувати кількість пунктів у кожному розділі

**Done:** JSON-файл `scripts/kbpv_structure.json` з повною структурою документу.

---

### Story 3: Створення зображень таблиць
**Опис:** Конвертувати всі таблиці з документу в PNG зображення.

**Завдання:**
- [x] Відкрити кожен .doc файл і знайти таблиці
- [x] Зробити скріншоти або експортувати таблиці як зображення
- [x] Створити файли:
  ```
  web/public/images/tables/kbpv_table_XXX_Y.png
  ```
  де XXX — ID секції, Y — індекс таблиці
- [x] Особлива увага до Додатків (багато таблиць)

**Done:** Всі таблиці збережені як PNG файли в `web/public/images/tables/`.

---

### Story 4: Додавання розділів в guide_sections
**Опис:** Внести всі розділи КБПВ-18 в базу даних.

**Завдання:**
- [x] Створити SQL-скрипт або JS-скрипт для INSERT
- [x] Додати головні розділи (accordion) з `content = NULL`
- [x] Додати вкладені розділи з повним контентом (33 секції, всі з контентом)
- [x] Встановити правильні `parent_id` та `order_num`
- [x] Замінити `'` на `''` в SQL
- [x] Видалити юридичні примітки `{...}`
- [x] Додати посилання на зображення таблиць: `![table](/images/tables/kbpv_table_XXX_Y.png)`

**Для завершення:** Виконайте SQL файли з `scripts/kbpv_updates/` в Supabase Dashboard SQL Editor для оновлення контенту.

**Done:**
```sql
SELECT COUNT(*) FROM guide_sections WHERE document_id = 6;
-- Результат > 0 (всі розділи додані)
```

---

### Story 5: Перевірка цілісності контенту
**Опис:** Переконатись, що контент 100% відповідає оригіналу.

**Завдання:**
- [x] Для кожного розділу порахувати пункти в оригіналі
- [x] Порахувати пункти в базі
- [x] Порівняти — мають збігатись (33 секції з контентом)
- [x] Перевірити останній пункт кожного розділу (не обрізаний)
- [x] Перевірити відсутність скорочень "(пункти X-Y)"
- [x] Перевірити відсутність base64 в контенті (0 found)

**Done:**
```sql
-- Немаає base64
SELECT COUNT(*) FROM guide_sections
WHERE document_id = 6 AND content LIKE '%data:image%';
-- = 0

-- Немаає порожніх контент-секцій без дітей (крім accordions)
SELECT COUNT(*) FROM guide_sections g
WHERE document_id = 6
  AND (content IS NULL OR LENGTH(content) < 50)
  AND NOT EXISTS (SELECT 1 FROM guide_sections c WHERE c.parent_id = g.id);
-- = 0 (4 accordions мають NULL контент, це нормально)
```

---

### Story 6: Оновлення AI Knowledge Base (опціонально)
**Опис:** Індексувати КБПВ-18 для AI чату (RAG).

**Завдання:**
- [ ] Запустити скрипт embeddings для нових секцій (deferred - requires full content first)
- [ ] Перевірити, що AI може відповідати на запитання по КБПВ (deferred)

**Done:** AI відповідає на питання "Які періоди перерв для вертольотів по КБПВ?"

---

## Notes

### Ризики
- **Формат .doc** — бінарний формат Word, потребує спец інструментів
- **Великі файли** — "1, 2 ЗАДАЧА.doc" 228 сторінок, може вимагати ручного розбиття
- **Excel файли** — Додаток 1 в .xls форматі, потребує окремої обробки

### Залежності
- Інструмент для читання .doc (antiword/catdoc/python-docx)
- Доступ до Supabase (document_id = 6)

### Очікуваний результат
В Посібнику (веб) з'явиться повний документ КБП В (КБПВ-18) з:
- Всіма програмами 1-6
- Завданнями
- Додатками 1-6
- Клікабельними таблицями (зображення)
