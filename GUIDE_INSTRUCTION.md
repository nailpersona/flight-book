# Інструкція: Додавання документів в Посібник

---

## 🤖 ДЛЯ AI АГЕНТІВ: ГОЛОВНІ ПРАВИЛА

### ✅ ГОЛОВНЕ ПРАВИЛО:
**Контент в базі ПОВИНЕН бути 100% ідентичним оригінальному документу.**

### 🚨 КРИТИЧНІ ЗАБОРОНИ:
1. **НЕ скорочувати** контент (наприклад: "(пункти 4-16)" — НЕДОПУСТИМО!)
2. **НЕ використовувати base64** для зображень — тільки файлові посилання
3. **НЕ пропускати** пункти в кінці розділу
4. **НЕ обрізати** речення посередині

---

## 📁 ДЕ ЗНАХОДЯТЬСЯ ОРИГІНАЛИ

| Документ | Файл |
|----------|------|
| ПВП ДАУ | `docs/pvp_dau_text.txt` |
| КЛПВ-24 | `docs/KLPV-24.md` або `docs/klpv24_text.txt` |
| КБП ВА | `docs/KBP-VA-2022.txt` |
| Інші | `docs/` папка |

---

## 📋 АЛГОРИТМ ДОДАВАННЯ ДОКУМЕНТА

### КРОК 1: Аналіз оригіналу

Перед додаванням обов'язково проаналізуй оригінал:

```
1. Яка структура документу? (розділи, підрозділи, додатки)
2. Чи є таблиці? → потрібно зберегти як зображення
3. Скільки пунктів у кожному розділі? → запиши для перевірки
```

### КРОК 2: Створення документу

```sql
INSERT INTO guide_documents (title, title_short, order_num, is_visible)
VALUES (
  'Повна назва документу',
  'Коротка назва',
  1,
  true
);
```

### КРОК 3: Додавання розділів

**Типи розділів:**

| Тип | Ознака | content | parent_id |
|-----|--------|---------|-----------|
| Accordion | має підрозділи | NULL | NULL або ID батька |
| Контент | кінцевий розділ | повний текст | NULL або ID батька |

**Приклад - головний розділ з підрозділами:**
```sql
-- Головний розділ (accordion)
INSERT INTO guide_sections (document_id, title, parent_id, order_num)
VALUES (3, 'ПРОГРАМА 1. Назва', NULL, 1);
-- content = NULL, бо цей розділ тільки групує підрозділи

-- Дізнайся ID
SELECT id FROM guide_sections WHERE title = 'ПРОГРАМА 1. Назва';
-- Припустимо: 805

-- Підрозділи (з контентом)
INSERT INTO guide_sections (document_id, title, content, parent_id, order_num)
VALUES
  (3, 'Розділ 1. Назва', 'Повний контент...', 805, 1),
  (3, 'Розділ 2. Назва', 'Повний контент...', 805, 2);
```

---

## 🖼️ РОБОТА З ТАБЛИЦЯМИ

### ⚠️ ВАЖЛИВО: Таблиці завжди як зображення!

**Чому?** Markdown-таблиці розїжджаються на різних екранах. Зображення зберігають структуру.

### Як додати таблицю:

**1. Створи зображення таблиці:**
- Зроби скріншот таблиці з оригіналу
- Або створи зображення з Word/Excel
- Збережи у форматі PNG

**2. Розмісти файл:**
```
web/public/images/tables/<doc>_<section>_<index>.png

Приклад:
web/public/images/tables/klpv_table_805_0.png  -- перша таблиця секції 805
web/public/images/tables/klpv_table_805_1.png  -- друга таблиця секції 805
```

**3. Додай посилання в контент:**
```sql
UPDATE guide_sections
SET content = '**ПЕРЕЛІК ВПРАВ:**

![table](/images/tables/klpv_table_805_0.png)

**Всього:** 78 польотів

---

## ДОПУСКИ

![table](/images/tables/klpv_table_805_1.png)'
WHERE id = 805;
```

### ❌ НЕПРАВИЛЬНО (base64):
```sql
-- НЕ роби так!
content = '![table](data:image/png;base64,iVBORw0KGgo...)'
```

### ✅ ПРАВИЛЬНО (файл):
```sql
-- Роби так!
content = '![table](/images/tables/klpv_table_805_0.png)'
```

---

## 📝 ФОРМАТУВАННЯ КОНТЕНТУ

### Markdown-синтаксис:

| Елемент | Синтаксис |
|---------|-----------|
| Жирний | `**текст**` |
| Курсив | `*текст*` |
| Заголовок | `## Заголовок` |
| Роздільник | `---` |
| Зображення | `![alt](/path/to/image.png)` |
| Список | `- пункт` або `1. пункт` |

### Обробка перед вставкою:

**ОБОВ'ЯЗКОВО:**
1. ✅ Замінити `'` на `''` (для SQL)
2. ✅ Видалити юридичні примітки `{...}`
3. ✅ Зберегти ВСІ пункти повністю
4. ✅ Перевірити, що контент не обрізаний

**ПРИКЛАД юридичної примітки (ВИДАЛИТИ):**
```
{Пункт 1 глави 2 розділу IX із змінами, внесеними згідно з Наказом...}
```

---

## ✅ ЧЕК-ЛІСТ ДОДАВАННЯ РОЗДІЛУ

### Перед INSERT:
- [ ] Прочитав оригінал повністю
- [ ] Порахував кількість пунктів (записав число: ____ )
- [ ] Визначив тип: accordion або контент
- [ ] Якщо є таблиці — підготував зображення

### Після INSERT:
- [ ] Отримав контент через SELECT
- [ ] Порахував пункти в базі
- [ ] Кількість збігається з оригіналом
- [ ] Перевірив останній пункт — не обрізаний
- [ ] Перевірив перший пункт — не обрізаний
- [ ] Немає заголовків наступних розділів

### SQL для перевірки:
```sql
SELECT id, title, LENGTH(content) as len, content
FROM guide_sections WHERE id = <ID>;

-- Порахувати пункти в content (шукай "**1.**", "**2.**" і т.д.)
```

---

## 🔴 ТИПОВІ ПОМИЛКИ

### Помилка 1: Скорочений контент
```
❌ НЕПРАВИЛЬНО:
"**4-16.** (Додаткові методичні вказівки)"

✅ ПРАВИЛЬНО:
"**4.** До польотів за вправами 101, 102 допускати...
**5.** Польоти за вправами 113, 114...
...
**16.** Рішення про допуск..."
```

### Помилка 2: Base64 зображення
```
❌ НЕПРАВИЛЬНО:
content LIKE '%data:image/png;base64%'

✅ ПРАВИЛЬНО:
content LIKE '%/images/tables/%'
```

### Помилка 3: Обрізаний останній пункт
```
❌ В базі пункт обривається на середині речення

✅ Кожен пункт закінчується повним реченням
```

### Помилка 4: Зайвий контент наступного розділу
```
❌ В кінці контенту є заголовок наступного розділу

✅ Контент закінчується перед заголовком наступного розділу
```

---

## 📊 ПЕРЕВІРКА ГОТОВОГО ДОКУМЕНТА

### SQL для перевірки структури:
```sql
SELECT
    id,
    title,
    LENGTH(content) as len,
    CASE
        WHEN content LIKE '%/images/tables/%' THEN '✓ зображення'
        WHEN content LIKE '%data:image%' THEN '✗ base64!'
        WHEN content IS NULL THEN '— accordion'
        ELSE '— текст'
    END as tables_status
FROM guide_sections
WHERE document_id = <ID>
ORDER BY order_num;
```

### Перевірка на скорочення:
```sql
-- Перевірити, чи немає скорочених вказівок
SELECT id, title, content
FROM guide_sections
WHERE document_id = <ID>
  AND content LIKE '%Додаткові методичні вказівки%';
-- Якщо є результати — треба розгорнути!
```

### Перевірка заповненості:
```sql
SELECT
    id,
    title,
    CASE
        WHEN content IS NULL OR LENGTH(content) < 50 THEN
            CASE
                WHEN EXISTS(SELECT 1 FROM guide_sections c WHERE c.parent_id = guide_sections.id)
                THEN '✅ Accordion'
                ELSE '❌ ПОРОЖНІЙ!'
            END
        ELSE '✅ OK'
    END as status
FROM guide_sections
WHERE document_id = <ID>
ORDER BY order_num;
```

---

## 🎯 ПОВНИЙ ПРИКЛАД: ДОДАВАННЯ КЛПВ

### 1. Аналіз оригіналу:
```
Структура КЛПВ-24:
- Загальні положення (пункти 1-33)
- Програма 1: 6 розділів, методичні вказівки (16 пунктів)
- Програма 2: 5 задач, кожна з методичними вказівками (20-28 пунктів)
- Програма 3: 4 розділи
- Програма 4: методичні вказівки (7 пунктів)
- Додатки 1-9: таблиці (потрібні зображення)
```

### 2. Створення документу:
```sql
INSERT INTO guide_documents (title, title_short, order_num)
VALUES ('Курс льотної підготовки випробувачів', 'КЛПВ', 2);
-- document_id = 3
```

### 3. Підготовка зображень таблиць:
```
Створено файли:
- web/public/images/tables/klpv_table_805_0.png (перелік вправ)
- web/public/images/tables/klpv_table_805_1.png (допуски)
- web/public/images/tables/klpv_table_825_0.png (перерви)
...
```

### 4. Додавання розділів:
```sql
-- Загальні положення (повний текст, пункти 1-33)
INSERT INTO guide_sections (document_id, title, content, order_num)
VALUES (3, 'ЗАГАЛЬНІ ПОЛОЖЕННЯ', '**1.** Організація...', 1);

-- Програма 1 (accordion + контент)
INSERT INTO guide_sections (document_id, title, content, parent_id, order_num)
VALUES (3, 'ПРОГРАМА 1...', '...повні 16 пунктів вказівок...
![table](/images/tables/klpv_table_805_0.png)
![table](/images/tables/klpv_table_805_1.png)', NULL, 2);
```

### 5. Перевірка:
```sql
-- Перевірити довжину контенту
SELECT title, LENGTH(content) FROM guide_sections WHERE document_id = 3;

-- Перевірити зображення
SELECT title, content LIKE '%/images/tables/%' as has_images
FROM guide_sections WHERE document_id = 3;

-- Перевірити відсутність base64
SELECT title FROM guide_sections
WHERE document_id = 3 AND content LIKE '%data:image%';
-- Має бути 0 результатів!
```

---

## 📐 СТРУКТУРА ТАБЛИЦЬ БАЗИ ДАНИХ

### guide_documents:
| Поле | Тип | Опис |
|------|-----|------|
| id | int | PK |
| title | text | Повна назва |
| title_short | text | Коротка назва для UI |
| order_num | int | Порядок відображення |
| is_visible | bool | true = показувати в Посібнику |

### guide_sections:
| Поле | Тип | Опис |
|------|-----|------|
| id | int | PK |
| document_id | int | FK → guide_documents |
| title | text | Заголовок розділу |
| content | text | Markdown-контент (NULL для accordion) |
| parent_id | int | FK → guide_sections (NULL = верхній рівень) |
| order_num | int | Порядок відображення |

---

## 🔄 ШВИДКИЙ DOCKER

### Додати розділ:
```sql
INSERT INTO guide_sections (document_id, title, content, parent_id, order_num)
VALUES (3, 'Назва', 'Контент...', NULL, 1);
```

### Оновити контент:
```sql
UPDATE guide_sections SET content = 'Новий контент...' WHERE id = 805;
```

### Перевірити структуру:
```sql
SELECT id, title, parent_id, LENGTH(content) FROM guide_sections
WHERE document_id = 3 ORDER BY order_num;
```

---

## ⚠️ ФІНАЛЬНИЙ ЧЕК-ЛІСТ

Перед завершенням роботи переконайся:

- [ ] **Всі розділи додані** — перевір через SELECT
- [ ] **Всі пункти присутні** — порахуй і порівняй з оригіналом
- [ ] **Таблиці як зображення** — немає base64
- [ ] **Немає скорочень** — немає "(додаткові пункти)"
- [ ] **Правильна структура** — parent_id встановлено вірно
- [ ] **Правильний порядок** — order_num відсортований

---

## 🆘 Якщо щось пішло не так

### Видалити розділ:
```sql
DELETE FROM guide_sections WHERE id = <ID>;
```

### Оновити контент:
```sql
UPDATE guide_sections SET content = 'Виправлений контент...' WHERE id = <ID>;
```

### Перевірити наявність зображень:
```sql
SELECT id, title FROM guide_sections
WHERE content LIKE '%/images/tables/%';
```

---

**Дотримуй цієї інструкції, і документи в Посібнику будуть на 100% відповідати оригіналам!**
