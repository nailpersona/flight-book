# Інструкція: Додавання контенту в Посібник

---

## 🤖 ДЛЯ AI АГЕНТІВ: КРИТИЧНІ ПРАВИЛА ПРИ ВИКОНАННІ ЗАВДАНЬ

Коли ти отримуєш завдання "додай розділ X" або "створи контент для розділу Y", ти ПОВИНЕН дотримуватися цих правил:

### ✅ ГОЛОВНЕ ПРАВИЛО:
**Контент в базі ПОВИНЕН бути 100% ідентичним оригінальному документу, крім юридичних приміток.**

### 📍 ДЕ БРАТИ ОРИГІНАЛ ДЛЯ ПЕРЕВІРКИ:

1. **ПВП ДАУ** (Правила випробувань польотами):
   - Файл: `C:\Users\fujitsu\Desktop\fly-book\docs\pvp_dau_text.txt`
   - Це текстова версія оригінального .docx документу
   - Використовуй Grep/Read для пошуку потрібного розділу

2. **Інші документи**:
   - Файли в папці `C:\Users\fujitsu\Desktop\fly-book\docs\`
   - КБП, КЛПВ, ПВП ДАУ тощо

### 📋 АЛГОРИТМ ВИКОНАННЯ ЗАВДАННЯ:

#### КРОК 1: Знайти оригінал в текстовому файлі
```bash
# Приклад: шукаємо розділ "Організація польотів"
grep -n "Організація польотів" docs/pvp_dau_text.txt

# Читаємо контент від знайденого рядка
read docs/pvp_dau_text.txt з рядка X до рядка Y
```

#### КРОК 2: Визначити межі розділу/підрозділу
- Знайти **початок** — заголовок розділу
- Знайти **кінець** — наступний заголовок (римська цифра = новий головний розділ, арабська цифра = новий підрозділ)

**ВАЖЛИВО:**
- **Римські цифри (I, II, III, IV, V, VI...)** = ГОЛОВНІ РОЗДІЛИ (parent_id = NULL)
- **Арабські цифри (1, 2, 3, 4, 5...)** = ПІДРОЗДІЛИ (parent_id = ID головного розділу)

#### КРОК 3: Порахувати пункти в оригіналі
- Порахувати всі пункти (1, 2, 3...) в оригінальному тексті
- Записати кількість — наприклад: "В оригіналі: 47 пунктів"

#### КРОК 4: Створити SQL з повним контентом
- Скопіювати ВСІ пункти з оригіналу
- **Видалити** юридичні примітки в фігурних дужках: `{...}`
- **Замінити** одинарні лапки на подвійні: `'` → `''`
- **НЕ обрізати** жодного речення!

#### КРОК 5: ОБОВ''ЯЗКОВА перевірка після додавання
```sql
-- Отримати щойно доданий контент
SELECT title, content FROM guide_sections WHERE id = <щойно доданий ID>;

-- Потім:
-- 1. Порахувати пункти в контенті
-- 2. Порівняти з кількістю в оригіналі
-- 3. Перевірити останній пункт — чи не обрізаний?
-- 4. Перевірити, що немає заголовка наступного розділу в кінці
```

### 🚨 ЧЕРВОНІ ПРАВИЛА (НИКОЛИ НЕ ПОРУШУЙ):

1. **Не пропускай пункти в кінці** — найчастіша помилка!
2. **Не обрізай речення посередині** — перевіряй кінцівку кожного пункту
3. **Не включай контент наступного розділу** — зупиняйся перед його заголовком
4. **Римські цифри = головні розділи**, арабські = підрозділи
5. **Юридичні примітки {...} видаляй**, все інше залишай

### ✅ ЧЕК-ЛІСТ ПЕРЕД ПОВЕРНЕННЯМ ВІДПОВІДІ:

- [ ] Знайшов і прочитав оригінал в `docs/pvp_dau_text.txt`
- [ ] Порахував пункти в оригіналі (записав число)
- [ ] Скопіював ВСІ пункти повністю
- [ ] Видалив юридичні примітки {...}
- [ ] Замінив ' на ''
- [ ] Додав через SQL
- [ ] Перевірив через SELECT, що контент додався
- [ ] Порахував пункти в базі — збігаються з оригіналом?
- [ ] Перевірив останній пункт — не обрізаний?
- [ ] Немає заголовка наступного розділу в кінці?

**Тільки після проходження всіх пунктів чек-ліста завдання вважається виконаним!**

---

## Структура посібника

Посібник (Guide) складається з двох таблиць в Supabase:
- `guide_documents` — головні документи (ПВП ДАУ, КБП тощо)
- `guide_sections` — розділи та підрозділи з контентом

## Правила поведінки

### Розділ з підрозділами (accordion)
- Натискаєш → розгортаються підрозділи
- **НЕМАЄ** контенту (`content` = NULL або пустий)
- Приклад: "ІІ. Екіпаж повітряного судна"

### Підрозділ з контентом
- Натискаєш → відкривається сторінка з матеріалом
- **МАЄ** контент (`content` заповнений)
- Приклад: "1. Склад екіпажу повітряного судна"

### Розділ без підрозділів (з контентом)
- Натискаєш → одразу відкривається сторінка з матеріалом
- **МАЄ** контент
- Приклад: "I. Загальні положення"

---

## Крок 1: Додати документ

Якщо документ ще не існує:

```sql
INSERT INTO guide_documents (title, title_short, order_num, is_visible)
VALUES (
  'Повне найменування документу',
  'Коротка назва',
  1,
  true   -- true = видно в Посібнику, false = приховано
);
```

**Примітка:**
- `is_visible = true` — документ відображається в Посібнику (для ПВП ДАУ, КЛПВ тощо)
- `is_visible = false` — документ приховано від UI (лише для AI knowledge base, наприклад КБП документи)

Приклад:
```sql
INSERT INTO guide_documents (title, title_short, order_num)
VALUES (
  'Правила випробувань польотами на аеродромах державної авіації України',
  'ПВП ДАУ',
  1
);
```

---

## Крок 2: Додати розділ без підрозділів (з контентом)

Приклад: "I. Загальні положення" — має тільки контент

```sql
INSERT INTO guide_sections (document_id, title, content, parent_id, order_num)
VALUES (
  1,                           -- document_id (ПВП ДАУ)
  'I. Загальні положення',     -- title
  'Текст контенту...',         -- content (повний текст!)
  NULL,                        -- parent_id (NULL = головний розділ)
  1                            -- order_num
);
```

---

## Крок 3: Додати розділ з підрозділами (accordion)

### Крок 3a: Додати головний розділ (БЕЗ контенту)

```sql
INSERT INTO guide_sections (document_id, title, parent_id, order_num)
VALUES (
  1,                           -- document_id
  'ІІ. Екіпаж повітряного судна',
  NULL,                        -- parent_id (головний розділ)
  2                            -- order_num
);
-- content залишаємо NULL - цей розділ тільки для групування підрозділів
```

### Крок 3b: Додати підрозділи (з контентом)

Спочатку дізнайся ID головного розділу:
```sql
SELECT id FROM guide_sections WHERE title = 'ІІ. Екіпаж повітряного судна';
-- Припустимо ID = 753
```

Додай підрозділи:
```sql
-- Підрозділ 1
INSERT INTO guide_sections (document_id, title, content, parent_id, order_num)
VALUES (
  1,                           -- той самий document_id
  '1. Склад екіпажу повітряного судна',
  'Текст про склад екіпажу...',
  753,                         -- parent_id = ID головного розділу
  1
);

-- Підрозділ 2
INSERT INTO guide_sections (document_id, title, content, parent_id, order_num)
VALUES (
  1,
  '2. Допуски льотного складу до польотів',
  'Текст про допуски...',
  753,                         -- той самий parent_id
  2
);
```

---

## Крок 4: Витягти текст з документу

### Де знаходяться оригінальні документи:

**ПВП ДАУ** (основний документ):
- Текстовий файл: `C:\Users\fujitsu\Desktop\fly-book\docs\pvp_dau_text.txt`
- Це конвертована версія оригінального .docx
- Використовуй цей файл для читання через Read/Grep інструменти

**Інші документи**:
- `C:\Users\fujitsu\Desktop\fly-book\docs\КБП ВА 2022.pdf`
- `C:\Users\fujitsu\Desktop\fly-book\docs\КЛПВ-24 еталон.docx`
- `C:\Users\fujitsu\Desktop\fly-book\docs\ПВП ДАУ наказ №2 від 05.01.2015.docx`

### Як витягти текст:

**Варіант 1: З текстового файлу (РЕКОМЕНДОВАНО для ПВП ДАУ)**

1. Знайди потрібний розділ через Grep:
   ```
   Пошук: "Організація польотів" в pvp_dau_text.txt
   ```
2. Прочитай контент від знайденого рядка
3. Скопіюй текст

**Варіант 2: З Word документу**

1. Відкрий документ у Word
2. Виділи потрібний розділ
3. Копіюй текст (Ctrl+C)
4. Встав у SQL-запит

### Обробка тексту перед вставкою в SQL:

**ОБОВ''ЯЗКОВО виконай:**
1. **Видалити юридичні примітки** в фігурних дужках: `{Наказ Міністерства оборони № 416 від 07.08.2017}`
2. **Замінити одинарні лапки на два одинарні:** `'` → `''`
3. **Перевірити, що всі пункти на місці** — порахуй їх!

**КРИТИЧНО:** Кінцевий текст ПОВИНЕН співпадати з оригінальним документом на 100%, крім юридичних приміток. Не пропускай пункти, не обрізай речення.

**Приклад юридичної примітки, ЯКУ ТРЕБА ВИДАЛИТИ:**
```
{Пункт 1 глави 1 розділу V із змінами, внесеними згідно з Наказом Міністерства оборони № 416 від 07.08.2017}
```

**Приклад заміни лапок:**
```
Текст: льотний склад САД ДА
SQL:   льотний склад САД ДА
```

---

## Крок 5: Перевірка повноти контенту (КРИТИЧНО!)

Після додавання контенту ОБОВ''ЯЗКОВО перевір повноту:

### Метод 1: Порівняння з оригінальним текстовим файлом

1. **Відкрий оригінал в `pvp_dau_text.txt`** через Read інструмент
2. **Знайди відповідний розділ** — використовуй Grep для пошуку заголовка
3. **Порахуй пункти в оригіналі** — запам''ятай число (наприклад: 47 пунктів)
4. **Отримай контент з бази:**
   ```sql
   SELECT title, content FROM guide_sections WHERE id = <ID>;
   ```
5. **Порахуй пункти в контенті бази** — число має збігатися!
6. **Порівняй:**
   - Перший пункт оригіналу == Перший пункт бази
   - Останній пункт оригіналу == Останній пункт бази
   - Кількість пунктів однакова

### Що перевіряти (Чек-ліст):

- [ ] **Кількість пунктів збігається** (рахуємо в оригіналі і в базі)
- [ ] **Перший пункт повний** (не обрізаний на початку)
- [ ] **Останній пункт повний** (не обрізаний в кінці!) ← **КРИТИЧНО!**
- [ ] **Немає пропущених пунктів** в середині (перевір номера: 1, 2, 3... без пропусків)
- [ ] **Немає заголовка наступного розділу** в кінці контенту
- [ ] **Немає юридичних приміток** {...} в контенті
- [ ] **Порядок пунктів правильний**

### 🔴 Найпоширеніші помилки:

#### Помилка А: Обрізаний останній пункт
```
В базі:
46. За повноту та якість підготовки до керівництва польотами осіб ГКрП відповідальність несе заступник командира авіаційної частини.

В оригіналі:
46. За повноту та якість підготовки до керівництва польотами осіб ГКрП відповідальність несе заступник командира авіаційної частини.
47. Командир авіаційної частини здійснює підготовку... ← ВІДСУТНІЙ В БАЗІ!
```

#### Помилка Б: Контент наступного розділу потрапив в попередній
```
В базі (неправильно):
...46. За повноту та якість...
V. Розвідка погоди                                    ← ЗАЙВИЙ ЗАГОЛОВОК!
1. Радіолокаційна розвідка погоди...                 ← ЗАЙВИЙ КОНТЕНТ!
```

#### Помилка В: Пропущені пункти в середині
```
В оригіналі: пункти 1, 2, 3, 4, 5
В базі:      пункти 1, 2, 3, 5 (пункт 4 відсутній)
```

### Метод 2: SQL-перевірка кількості пунктів

```sql
-- Отримати контент
SELECT title, content FROM guide_sections WHERE id = <ID>;

-- Потім вручну порахувати пункти в content:
-- Шукай патерни: "^1.", "^2.", "^3." і т.д.
```

### Метод 3: Пошук заголовків в контенті (перевірка на зараження)

```sql
-- Перевірити, чи немає в контенті заголовків інших розділів
SELECT title, content
FROM guide_sections
WHERE id = <ID>
  AND (
    content LIKE '%V. %'      -- перевірка на заголовок розділу V
    OR content LIKE '%VI. %'   -- перевірка на заголовок розділу VI
    OR content LIKE '%. %'     -- перевірка на наступні підрозділи
  );
-- Якщо результат є — контент заражений, треба очистити
```

---

## Крок 6: Обов''язкова перевірка через SQL (ЗАВЖДИ виконуй!)

Після додавання кожного розділу/підрозділу ОБОВ''ЯЗКОВО перевір контент через SQL:

```sql
-- Отримати контент для перевірки
SELECT title, content
FROM guide_sections
WHERE id = <ID щойно доданого розділу>;
```

**ЧЕРВОНІ ПРАВИЛА (ніколи не порушуй!):**

1. **Перевірай кінцівку кожного пункту:**
   - Пункт не повинен обриватися на середині речення
   - Після останнього пункту не повинно йти контент наступного підрозділу
   - Кожен пункт має закінчуватися повним реченням

2. **Рахуй пункти:**
   - Порахуй кількість пунктів в оригіналі
   - Порахуй кількість пунктів в контенті бази
   - Числа мають збігатися!

3. **Перевірай межі підрозділів:**
   - Переконайся, що контент закінчується перед заголовком наступного підрозділу
   - Немає "зайвих" абзаців в кінці

**Приклад перевірки:**
```sql
-- Приклад: перевіряємо підрозділ 3 розділу III
-- В оригіналі: 7 пунктів (1-7)
-- В базі має бути також 7 пунктів

SELECT title, content
FROM guide_sections
WHERE id = 785;  -- Перевірки осіб групи керівництва польотами

-- Потім:
-- 1. Скопіюй content в текстовий редактор
-- 2. Порахуй кількість пунктів (шукай "^1.", "^2.", "^3." і т.д.)
-- 3. Знайди останній пункт - чи не обривається він?
-- 4. Порівняй з оригінальним документом
```

---

## Приклад: Повний цикл для "I. Загальні положення"

```sql
-- 1. Перевір, чи існує документ
SELECT * FROM guide_documents WHERE title_short = 'ПВП ДАУ';

-- 2. Додай розділ з контентом
INSERT INTO guide_sections (document_id, title, content, order_num)
VALUES (
  1,
  'I. Загальні положення',
  '**1.** Ці Правила визначають порядок організації та проведення польотів державної авіації (далі - ДА)...',
  1
);
```

---

## Приклад: Розділ з підрозділами

```sql
-- 1. Додай головний розділ
INSERT INTO guide_sections (document_id, title, parent_id, order_num)
VALUES (1, 'ІІ. Екіпаж повітряного судна', NULL, 2);

-- 2. Дізнайся ID
SELECT id FROM guide_sections WHERE title = 'ІІ. Екіпаж повітряного судна';
-- Припустимо: 753

-- 3. Додай підрозділи
INSERT INTO guide_sections (document_id, title, content, parent_id, order_num)
VALUES
  (1, '1. Склад екіпажу повітряного судна', 'Текст...', 753, 1),
  (1, '2. Допуски льотного складу до польотів', 'Текст...', 753, 2),
  (1, '3. Обов''язки командира екіпажу', 'Текст...', 753, 3);
```

---

## Перевірка

```sql
-- Перевір структуру
SELECT
  s.id,
  s.title,
  s.parent_id,
  s.order_num,
  CASE WHEN s.content IS NULL THEN 'accordion' ELSE 'content' END as type
FROM guide_sections s
WHERE s.document_id = 1
ORDER BY s.order_num;
```

---

## Форматування контенту

Для markdown-форматування в контенті:
- **Жирний**: `**текст**`
- *Курсив*: `*текст*`
- Новий абзац: порожній рядок

Приклад:
```sql
'**1.** Це заголовок

Це абзац з текстом.

**2.** Це інший заголовок

Продовження тексту...'
```

---

## SQL через Supabase Dashboard

1. Відкрий https://supabase.com/dashboard
2. Вибери проект `klqxadvtvxvizgdjmegx`
3. SQL Editor → New query
4. Встав SQL-код
5. Run

---

## 🔴 Реальні помилки та як їх уникнути

### Помилка #1: Обрізаний пункт

**Проблема:** Пункт 5 закінчується на середині речення:
```
5. Перевірка осіб ГКрП у керівництві польотами здійснюється їх безпосередніми і старшими над ними начальниками, які мають відповідну підготовку і допуск до керівництва польотами.
```
**Мало б бути:**
```
5. Перевірка осіб ГКрП у керівництві польотами здійснюється їх безпосередніми і старшими над ними начальниками, які мають відповідну підготовку і допуск до керівництва польотами.
Якщо рівень підготовки начальників не відповідає зазначеним вимогам, перевірку осіб ГКрП здійснюють інші підготовлені посадові особи, які призначені ними.
```

**Як уникнути:** Завжди перевіряй останній речення кожного пункту - чи закінчується воно логічно.

---

### Помилка #2: Контент наступного підрозділу потрапив в попередній

**Проблема:** В кінці підрозділу 2 з''явився контент підрозділу 3:
```sql
-- Підрозділ 2 закінчується словами:
"Відновлення допуску здійснюється в тому самому порядку, що і при його отриманні."

-- Але в контенті є додатково:
"3. Перевірки осіб групи керівництва польотами
1. Усі особи ГКрП..."
```

**Як уникнути:**
1. Перед додаванням контенту перевір, чи немає в ньому заголовків наступних підрозділів
2. Контент кожного підрозділу має закінчуватися перед заголовком наступного
3. Використовуй SQL-перевірку після додавання

---

### Помилка #3: Пропущені пункти в кінці підрозділу

**Проблема:** В підрозділі 4 є тільки пункти 1-3, а в оригіналі - ще пункт 4:
```
В базі: пункти 1, 2, 3
В оригіналі: пункти 1, 2, 3, 4
```

**Як уникнути:**
1. Перед додаванням порахуй пункти в оригіналі
2. Після додавання порахуй пункти в базі
3. Числа мають збігатися!

---

## Чек-лист перед завершенням роботи

- [ ] Порахував пункти в оригіналі
- [ ] Порахував пункти в доданому контенті
- [ ] Числа збігаються
- [ ] Перевірив кінцівку останнього пункту (не обрізана)
- [ ] Перевірив, що немає заголовків наступних підрозділів в контенті
- [ ] Виконав SQL-перевірку контенту
- [ ] Порівняв з оригінальним документом візуально

---

## Нюанси

1. **order_num** — важливий! Визначає порядок відображення
2. **parent_id**:
   - `NULL` = головний розділ (верхній рівень)
   - число = підрозділ (всередині іншого розділу)
3. **content**:
   - Є контент → натискання відкриває сторінку з матеріалом
   - Немає контенту → натискання розгортає підрозділи
4. **document_id** — один і той самий для всіх розділів одного документа
5. **Повнота контенту** — завжди порівнюй з оригінальним документом. Контент в базі має бути 100% ідентичним оригіналу, включаючи всі підпункти, абзаци та примітки (крім юридичних приміток у фігурних дужках)
6. **🔴 Римські цифри в заголовках (I, II, III, IV, V, VI...) — це ГОЛОВНІ РОЗДІЛИ, а не підрозділи!**
   - Якщо заголовок містить римську цифру (наприклад "V. Розвідка погоди", "VI. Передпольотна підготовка") — це самостійний головний розділ
   - Такі розділи НЕ додавай як підрозділи до інших розділів
   - У них parent_id має бути NULL
   - Приклад: "V. Розвідка погоди" — це окремий розділ, НЕ підрозділ "IV. Організація польотів"

---

## 🔄 ПОЛНИЙ РОБОЧИЙ ПРОЦЕС (Quick Reference)

### Схема: Головний розділ з підрозділами

```
IV. Організація польотів (головний розділ, accordion, parent_id=NULL)
├── 1. Зміст організації польотів (підрозділ, з контентом, parent_id=ID_IV)
├── 2. Прийняття рішення... (підрозділ, з контентом, parent_id=ID_IV)
├── 3. Планування польотів (підрозділ, з контентом, parent_id=ID_IV)
└── 4. Підготовка до польотів... (підрозділ, з контентом, parent_id=ID_IV)

V. Розвідка погоди (окремий головний розділ, accordion, parent_id=NULL)
├── 1. Радіолокаційна розвідка погоди
└── 2. Повітряна розвідка погоди
```

### Алгоритм дій при завданні "Додай розділ X":

1. **Пошук в оригіналі:**
   ```
   Grep: "IV. Організація польотів" в docs/pvp_dau_text.txt
   ```

2. **Визначення типу розділу:**
   - Римська цифра (IV, V, VI...) → головний розділ → parent_id = NULL
   - Арабська цифра (1, 2, 3...) → підрозділ → parent_id = ID головного

3. **Читання контенту:**
   ```
   Read: docs/pvp_dau_text.txt від рядка X до рядка Y
   ```

4. **Обробка:**
   - Видалити {...} юридичні примітки
   - Замінити ' на ''
   - Порахувати пункти

5. **Додавання в SQL:**
   ```sql
   INSERT INTO guide_sections (document_id, title, content, parent_id, order_num)
   VALUES (1, 'Title', 'Content...', NULL або ID, order_num);
   ```

6. **Перевірка (ОБОВ''ЯЗКОВА):**
   ```sql
   SELECT title, content FROM guide_sections WHERE id = <новий ID>;
   ```
   - Порахувати пункти
   - Порівняти з оригіналом
   - Перевірити останній пункт

7. **Фінал:**
   - Якщо все збігається → Готово
   - Якщо є розбіжності → Виправити і повторити перевірку

---

## 📁 Швидкий пошук в оригіналі (приклади)

```bash
# Знайти розділ за назвою
grep -n "Організація польотів" docs/pvp_dau_text.txt

# Знайти всі розділи з римськими цифрами
grep -n "^[IVXLCDMD]\+\." docs/pvp_dau_text.txt

# Знайти всі підрозділи з арабськими цифрами
grep -n "^[0-9]\+\." docs/pvp_dau_text.txt
```

---

---

## 🔍 АВТОМАТИЧНА ПЕРЕВІРКА ЗАПОВНЕНОСТІ ДОКУМЕНТІВ

### Обов'язкова перевірка після завершення роботи

Після завершення роботи з документом ОБОВ''ЯЗКОВО виконай перевірку:

```sql
-- Перевірка повноти заповнення ПВП ДАУ (document_id = 1)
SELECT
    id,
    title,
    LENGTH(content) as content_length,
    CASE
        WHEN content IS NULL OR LENGTH(TRIM(content)) < 50 THEN '❌ Порожній'
        ELSE '✅ Заповнений'
    END as status,
    parent_id
FROM guide_sections
WHERE document_id = 1
ORDER BY order_num, id;
```

### Критерії оцінки:

| Статус | Критерій | Дія |
|--------|----------|-----|
| ✅ Заповнений | content_length ≥ 50 | OK |
| ⚪ Батьківський | content IS NULL, є діти | OK (accordion) |
| ❌ Порожній | content_length < 50 і немає дітей | **ПОТРІБНО ЗАПОВНИТИ** |

### Перевірка наявності підрозділів у порожніх розділах:

```sql
-- Перевірити чи мають "порожні" розділи підрозділи
SELECT
    parent.id as parent_id,
    parent.title as parent_title,
    COUNT(child.id) as children_count,
    CASE
        WHEN COUNT(child.id) > 0 THEN '✅ Accordion (є підрозділи)'
        ELSE '❌ ПОТРІБЕН КОНТЕНТ!'
    END as status
FROM guide_sections parent
LEFT JOIN guide_sections child ON child.parent_id = parent.id
WHERE parent.document_id = 1
  AND (parent.content IS NULL OR LENGTH(TRIM(parent.content)) < 50)
GROUP BY parent.id, parent.title
ORDER BY parent.order_num;
```

---

## 📐 ПРАВИЛА РОЗБИТТЯ НА ПІДРОЗДІЛИ

### Коли розбивати розділ на підрозділи:

1. **Розділ має внутрішню структуру в оригіналі**
   - Якщо в оригіналі є підзаголовки типу "1. Підрозділ", "2. Підрозділ" → розбивай

2. **Розділ занадто великий**
   - Якщо content_length > 50,000 символів → розглянь розбиття
   - Особливо для розділів з функціональними обов'язками (XVII-XX)

3. **Логічна структура документу**
   - Якщо інші розділи такого ж рівня мають підрозділи → розбивай

### Як розбити існуючий розділ:

```sql
-- КРОК 1: Створити новий батьківський розділ (accordion)
UPDATE guide_sections
SET content = NULL
WHERE id = <ID_розбиваного_розділу>;

-- КРОК 2: Додати підрозділи
INSERT INTO guide_sections (document_id, title, content, parent_id, order_num)
VALUES
    (1, '1. Назва підрозділу', 'Контент першої частини...', <ID_батька>, 1),
    (1, '2. Назва підрозділу', 'Контент другої частини...', <ID_батька>, 2);
```

### Ознаки, що розділ треба розбити:

| Ознака | Приклад | Дія |
|--------|---------|-----|
| Є підзаголовки "1. ...", "2. ..." | "IX. Особливості..." має глави 1-18 | Розбити на підрозділи |
| Content > 50,000 символів | Розділ XVI (63,510 символів) | Розбити за підзаголовками |
| Багато різних тем | "Особливості..." — 18 різних видів польотів | Розбити за темами |

---

## 🎯 100% ВІДПОВІДНІСТЬ ОРИГІНАЛУ

### Головне правило:

> **Контент в базі = 100% текст оригіналу МІНУС юридичні примітки**

### Що ВИДАЛЯТИ з оригіналу:

```
{Пункт 1 глави 2 розділу IX із змінами, внесеними згідно з Наказом...}
{Абзац третій пункту 5 глави 1 розділу X в редакції Наказу...}
{У тексті глави 10 скорочення "ПРР" замінено...}
```

### Що ЗАЛИШАТИ без змін:

- ✅ Всі пункти (1, 2, 3...)
- ✅ Всі підпункти (1), 2), 3)...)
- ✅ Всі абзаци
- ✅ Всі терміни та визначення
- ✅ Всі таблиці та схеми
- ✅ Нумерацію пунктів
- ✅ Формулювання речень

### Перевірка відповідності:

```sql
-- Отримати контент для порівняння з оригіналом
SELECT title, content FROM guide_sections WHERE id = <ID>;

-- Потім:
-- 1. Відкрий оригінал в docs/pvp_dau_text.txt
-- 2. Знайди відповідний розділ
-- 3. Порівняй пункт за пунктом
-- 4. Всі формулювання мають збігатися дослівно
```

---

## 📋 ФІНАЛЬНИЙ ЧЕК-ЛІСТ ПЕРЕД ЗАВЕРШЕННЯМ

### Для кожного документа:

- [ ] **Перевір структуру** — всі розділи на місці?
- [ ] **Перевір заповненість** — немає порожніх без підрозділів?
- [ ] **Перевір відповідність** — контент = оригінал мінус юридичні примітки?
- [ ] **Перевір підрозділи** — великі розділи розбиті?
- [ ] **Перевір order_num** — правильний порядок відображення?
- [ ] **Перевір parent_id** — правильна ієрархія?

### SQL для фінальної перевірки:

```sql
-- Повний звіт по документу
SELECT
    CASE
        WHEN parent_id IS NULL THEN '📁 Головний'
        ELSE '   📄 Підрозділ'
    END as level,
    title,
    LENGTH(content) as chars,
    CASE
        WHEN content IS NULL OR LENGTH(TRIM(content)) < 50 THEN
            CASE
                WHEN EXISTS(SELECT 1 FROM guide_sections c WHERE c.parent_id = guide_sections.id)
                THEN '✅ Accordion'
                ELSE '❌ ПОРОЖНІЙ!'
            END
        ELSE '✅ OK'
    END as status
FROM guide_sections
WHERE document_id = <document_id>
ORDER BY
    COALESCE(parent_id, id),
    order_num;
```

---

## ⚠️ ПРИМІТКА ДЛЯ КОРИСТУВАЧА

Коли ти даєш завдання AI-агенту додати розділ, вказуй:

**Погано:** "Додай розділ про погоду"

**Добре:** "Додай розділ V 'Розвідка погоди' з ПВП ДАУ. Оригінал в docs/pvp_dau_text.txt, рядок ~595. Це головний розділ (parent_id=NULL) з двома підрозділами: 1) Радіолокаційна розвідка погоди, 2) Повітряна розвідка погоди."

**Найкраще:** "Додай розділ V 'Розвідка погоди' та його підрозділи. Виконай повну перевірку згідно з інструкцією."

### Для перевірки існуючого документа:

**Команда:** "Перевір ПВП ДАУ на повноту заповнення. Якщо є порожні розділи без підрозділів — заповни їх з оригіналу docs/pvp_dau_text.txt"
